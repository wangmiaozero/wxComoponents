<!--index.wxml-->
<view class="container">
  <block class="container-child" wx:for="{{items}}" wx:key="{{item.id}}">
    <navigator class="item container-child" url="{{item.navigateUrl}}">{{item.title}}</navigator>
  </block>
</view>
